<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <!-- SAFER check for pageTitle -->
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Laptop Exchange' %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
        referrerpolicy="no-referrer" />
</head>
<body>
<header>
  <div class="header-content">
    <a href="/"><img src="https://static.vecteezy.com/system/resources/thumbnails/018/743/179/small/laptop-computer-with-blank-transparent-screen-and-background-format-png.png" class="logo-img" alt="Logo"></a>

    <div class="search-bar">
      <form action="/items" method="get">
        <!-- SAFER check for searchTerm -->
        <input type="text" name="search" placeholder="Search..."
               value="<%= typeof searchTerm !== 'undefined' ? searchTerm : '' %>"/>
        <button type="submit">Go</button>
      </form>
    </div>

    <nav>
      <ul>
        <li><a href="/items">Browse</a></li>
        <li><a href="/items/new">Sell</a></li>

        <% if (!currentUserId) { %>
          <li><a href="/users/new">Sign Up</a></li>
          <li><a href="/users/login">Login</a></li>
        <% } else { %>
          <li><a href="/users/profile">My Account</a></li>
          <li><a href="/users/logout">Log Out</a></li>
        <% } %>
      </ul>
    </nav>
  </div>
</header>

<!-- Flash messages -->
<% if (success.length) { %>
  <div class="flash-success"><%= success %></div>
<% } %>
<% if (error.length) { %>
  <div class="flash-error"><%= error %></div>
<% } %>